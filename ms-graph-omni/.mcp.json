{
  "mcpServers": {
    "ms-graph-omni": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "_comment_required": "Set MSGO_TENANT_ID, MSGO_CLIENT_ID, MSGO_KEY_VAULT_URL, MSGO_CERT_NAME in your shell or override this .mcp.json per-machine. Or copy config/runtime.json.example to config/runtime.json and fill in values. The server fails fast with a missing-vars error if none are set.",
        "MSGO_TOKEN_CACHE_DIR": "${USERPROFILE}/.msgo-cache",
        "MSGO_LOG_LEVEL": "info"
      }
    }
  }
}
